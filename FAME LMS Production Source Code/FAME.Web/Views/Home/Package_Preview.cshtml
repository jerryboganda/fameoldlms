@model First_Aid_Made_Easy.Models.PackageVM
@{
    ViewBag.Title = "Course Preview";
    int i = 0;
}
<div class="page-section bg-alt border-bottom-2">
    <div class="container page__container">

        <div class="d-flex flex-column flex-lg-row align-items-center">
            <div class="d-flex flex-column flex-md-row align-items-center flex mb-16pt mb-lg-0 text-center text-md-left">
                <div class="flex">
                    <h1 class="h2 m-0">@Model.PackageName</h1>
                </div>
            </div>
            <div class="ml-lg-16pt">
                <a href="/Home/Packages" class="btn btn-light">All Packages</a>
            </div>
        </div>

    </div>
</div>

<div class="page-section border-bottom-2">
    <div class="container page__container">

        <div class="row">
            <div class="col-lg-12">
                <div class="row mb-24pt">
                    <div class="page-separator">
                        <div class="page-separator__text">Courses You'll Get</div>
                    </div>

                    <div class="row">
                        @foreach (var d in Model.Detail)
                        {
                            <div class="col-md-6">
                                <ul class="list-unstyled">
                                    <li class="d-flex align-items-center">
                                        <a href="/Home/Course_Preview/@d.CourseID">
                                            <img style="height:70px; width:70px" src="@Url.Content("~/Images/"+d.CoursePic)" />
                                            <span class="text-70">&nbsp; @d.CourseName</span>
                                        </a>
                                    </li>
                                </ul>
                            </div>
                        }
                    </div>
                </div>
            </div>
            <div class="col-lg-6">
                <div class="page-separator">
                    <div class="page-separator__text">Table of Durations</div>
                </div>
                <div class="accordion js-accordion accordion--boxed " id="parent">
                    @if (Model.Durations.Count <= 0)
                    {
                        <div class="accordion__menu-link">
                            <span class="text-accent-red">Sorry No Duration Available For This Package</span>
                        </div>
                    }

                    @foreach (var s in Model.Durations)
                    {
                        i++;
                        <div class="accordion__item open">
                            <a href="#" class="accordion__toggle" data-toggle="collapse" data-target="#course-toc-@i" data-parent="#parent">
                                <span class="flex">@s.DurationChar</span>
                                <span class="">Rs. @s.Price</span>
                            </a>
                        </div>
                    }
                </div>

                <div class="modal fade" id="PaymentModal" tabindex="-1" role="dialog" aria-labelledby="PaymentModalLabel" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="PaymentModalLabel">Choose Your Payment Method</h5>
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                            <div class="modal-body">
                                @*"/Payment/PayByEasyPaisa"*@
                                <a href="#" class="btn btn-light btn-block mb-24pt">
                                    <span class="fab fa-exchange-alt icon--left"></span>
                                    Continue with EasyPaisa
                                </a>
                                <a href="#" class="btn btn-light btn-block mb-24pt">
                                    <span class="fab fa-java icon--left"></span>
                                    Continue with JazzCash
                                </a>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



@section scripts{
    <script>
        $(document).ready(function () {
            $(".preloader").hide();
            $(".accordion__toggle").trigger("click");
        })

        $("#btnplayVideo").click(function () {
            var CourseID = $(this).data().id;
            loadVideo(CourseID);
        })



        $('#PaymentModal').on('show.bs.modal', function (event) {
            var button = $(event.relatedTarget) // Button that triggered the modal
            var recipient = button.data('whatever') // Extract info from data-* attributes
            // If necessary, you could initiate an AJAX request here (and then do the updating in a callback).
            //Update the modal's content. We'll use jQuery here, but you could use a data binding library or other methods instead.
            var modal = $(this)
            modal.find('.modal-title').text('New message to ' + recipient)
            modal.find('.modal-body input').val(recipient)
        })

        $(document).on("click", "#btnUnlockSec ", function () {
            var id = $(this).data("id");
            Enroll("ST", id);
        })
        $(document).on("click", "#btnUnlockCo", function () {
            var id = $(this).data("id");
            Enroll("CR", id);
        })
        function Enroll(For, id) {
            Swal.fire({
                title: 'Enter Your Coupon Secret',
                input: 'text',
                inputAttributes: {
                    autocapitalize: 'off'
                },
                showCancelButton: true,
                confirmButtonText: 'Submit',
                showLoaderOnConfirm: true,
                preConfirm: (login) => {
                    return fetch(`/Enrollment/Enroll/${id}?Method=CP&EnrollFor=${For}&Secret=${login}`, {
                        method: 'POST',
                    })
                        .then(response => {
                            if (!response.ok) {
                                throw new Error(response.statusText)
                            }
                            return response.json()
                        })
                        .catch(error => {
                            Swal.showValidationMessage(
                                `Request failed: ${error}`
                            )
                        })
                },
                allowOutsideClick: () => !Swal.isLoading()
            }).then((result) => {
                if (result.isConfirmed) {
                    if (result.value == "Success") {
                        Swal.fire({
                            title: `Success`,
                            text: 'Enrollment Succefull Please Visit "Our Courses" Page.Thank You',
                        })
                        window.location.href = "/Student/MyCourses";
                    }
                    if (result.value == "Error") {
                        Swal.fire({
                            title: `Error`,
                            text: 'Sorry! Something Went Wrong',
                        })
                    }
                    if (result.value > 0) {
                        Swal.fire({
                            title: `Success`,
                            text: 'Your Amount IS Discounted by ' + result.value + "%",
                        })
                    }
                }
            })
        }
    </script>
    <script>
    @* function Enroll(For, id) {
        const swalWithBootstrapButtons = Swal.mixin({
            customClass: {
                confirmButton: 'btn btn-success',
                cancelButton: 'btn btn-danger'
            },
            buttonsStyling: false
        })

        swalWithBootstrapButtons.fire({
            title: 'How You Want To Pay',
            icon: 'question',
            showCancelButton: true,
            confirmButtonText: 'Pay By EasyPaisa',
            cancelButtonText: 'Pay By JazzCash',
            reverseButtons: true
        }).then((result) => {
            if (result.isConfirmed) {

                $.ajax({
                    url: "/Enrollment/Enroll/",
                    type: "GET",
                    data: {
                        id: id,
                        Method: method,
                        EnrollFor: For, //Course/Section
                        Secret: "",
                    },
                    success: function (r) {
                        switch (r) {
                            case "Login":
                                var urlpost = "@Url.Action("Login","Account",new {returnUrl = " / Home / Course_Preview / CourseID" })";
                                window.location.href = urlpost.replace("CourseID", "@Model.Course_Id");
                                break;
                            case "Already":
                                swalWithBootstrapButtons.fire(
                                    'Accepted!',
                                    'You Have Already Enrolled This Section.',
                                    'warning'
                                )
                                break;
                            case "success":
                                swalWithBootstrapButtons.fire(
                                    'Accepted!',
                                    'Your request has been Transmitted.',
                                    'success'
                                )
                                break;
                            default:
                                swalWithBootstrapButtons.fire(
                                    'Sorry!',
                                    'Something Went Wrong',
                                    'error'
                                )
                        }
                    },
                    Error: function (err) {
                        swalWithBootstrapButtons.fire(
                            'Sorry!',
                            err,
                            'error'
                        )
                    }
                })

                /* Read more about handling dismissals below */
            } else if (result.dismiss === Swal.DismissReason.cancel) {
                $.ajax({
                    url: "/Enrollment/JC" + Action + "/" + id,
                    type: "GET",
                    success: function (r) {
                        switch (r) {
                            case "Login":
                                var urlpost = "@Url.Action("Login","Account",new {returnUrl = " / Home / Course_Preview / CourseID" })";
                                window.location.href = urlpost.replace("CourseID", "@Model.Course_Id");
                                break;
                            case "Already":
                                swalWithBootstrapButtons.fire(
                                    'Accepted!',
                                    'You Have Already Enrolled This Section.',
                                    'warning'
                                )
                                break;
                            case "success":
                                swalWithBootstrapButtons.fire(
                                    'Accepted!',
                                    'Your request has been Transmitted.',
                                    'success'
                                )
                                break;
                            default:
                                swalWithBootstrapButtons.fire(
                                    'Sorry!',
                                    'Something Went Wrong',
                                    'error'
                                )
                        }
                    },
                    Error: function (err) {
                        swalWithBootstrapButtons.fire(
                            'Sorry!',
                            err,
                            'error'
                        )
                    }
                })
            }
        })
    } *@
                        //Swal.fire({
                        //    title: 'Are you sure?',
                        //    text: "You won't be able to revert this!",
                        //    icon: 'warning',
                        //    showCancelButton: true,
                        //    confirmButtonColor: '#3085d6',
                        //    cancelButtonColor: '#d33',
                        //    confirmButtonText: 'Yes, delete it!'
                        //}).then((result) => {
                        //    if (result.isConfirmed) {
                        //        Swal.fire(
                        //            'Deleted!',
                        //            'Your file has been deleted.',
                        //            'success'
                        //        )
                        //    }
                        //})

                        //Swal.fire({
                        //    title: 'Sweet!',
                        //    text: 'Modal with a custom image.',
                        //    imageUrl: 'https://unsplash.it/400/200',
                        //    imageWidth: 400,
                        //    imageHeight: 200,
                        //    imageAlt: 'Custom image',
                        //})
    </script>
}