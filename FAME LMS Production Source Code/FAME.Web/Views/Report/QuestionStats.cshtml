@model First_Aid_Made_Easy.Models.StudentVM
@{
    ViewBag.Title = "Admin";
    Layout = "~/Views/Shared/_LayoutTeacher.cshtml";
}
<!-- Select2 -->
<link type="text/css" href="~/Content/assets/css/select2.css" rel="stylesheet">
<link type="text/css" href="~/Content/assets/vendor/select2/select2.min.css" rel="stylesheet">

<!-- DateRangePicker -->
<link type="text/css" href="~/Content/assets/vendor/daterangepicker.css" rel="stylesheet">

<div class="pt-32pt">
    <div class="page__container d-flex flex-column flex-md-row align-items-center text-center text-sm-left">
        <div class="flex d-flex flex-column flex-sm-row align-items-center">
            <div class="mb-24pt mb-sm-0 mr-sm-24pt">
				<h2 class="mb-0">Question Stats Report </h2>
            </div>
        </div>
    </div>
    <div class="page-section border-bottom-2">
        <div style="margin: 0; max-width: 100%" class="container-fluid page__container">
            <div class="row">
                <div class="col-md-12">
                    <div class="page-separator">
                        <div class="page-separator__text">Search Filters</div>
                    </div>
                    <div class="row" style="margin-bottom:20px !important">

                        <div class="col-md-12 mb-10" style="margin-bottom:20px">
                            <div class="text-muted">
                                Lists all questions and shows how many times each was attempted, skipped (omitted), answered correctly, and answered incorrectly. <br />
                                It can also filter results by system or specific question paper.
                            </div>
                        </div>
                        <div class="col-md-4">
                            <label>Question Paper</label>&emsp;
                            @Html.DropDownList("PaperID", new SelectList(ViewBag.Papers, "Value", "Text"), "Please Select", new { @class = "form-control select2", @data_toggle = "select" })
                        </div>
                        <div class="col-md-4">
                            <label>System</label>&emsp;
                            @Html.DropDownList("SystemID", new SelectList(ViewBag.Systems, "ID", "Name"), "Please Select", new { @class = "form-control select2", @data_toggle = "select" })
                        </div>
                        <div style="margin-top:25px" class="text-right col-md-4">
                            <button id="btnShow" class="btn btn-outline-primary" onclick="ShowReport(false)">Preview Report</button>
                            <button id="btnShow" class="btn btn-outline-primary" onclick="ShowReport(true)">Export To PDF</button>
                            <button id="btnShow" class="btn btn-outline-primary" onclick="ShowReport(false , true)">Export To Excel</button>
                        </div>
                    </div>
                </div>
                <div class="col-md-12 card" style="min-height: 400px; padding: 30px;" id="divReport">

                </div>
            </div>
        </div>
    </div>
</div>



@section scripts {
    @if (TempData["Success"] != null)
    {
        <script>
            toastr.success("Data Saved Successfully");
        </script>
    }

    <script>

        function ShowReport(IsPDF, IsExcel) {
            var PaperID = $("#PaperID").val() || 0;
			var SystemID = $("#SystemID").val() || 0;
			if (SystemID > 0 || PaperID > 0) {
				var SystemName = $("#SystemID option:selected").text();
				var url = `/Report/GetQuestionStats?PaperID=${PaperID}&SystemID=${SystemID}&IsPDF=${IsPDF}&IsExcel=${IsExcel}&SystemName=${SystemName}`
                if (IsPDF || IsExcel)
                    window.open(url, "_blank");
                else {
                    $(".divLoader").show();
                    $.ajax({
                        url: url,
                        success: function (html) {
                            $("#DTable").DataTable().destroy();
                            $("#divReport").html(html);
                            $("#DTable").DataTable();
                            $(".divLoader").hide();
                        }
                    })
                }
            }
        }
    </script>

}

