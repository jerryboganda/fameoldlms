@model First_Aid_Made_Easy.Models.QuestionVM
@using First_Aid_Made_Easy.BLL
@{
    Layout = "~/Views/Shared/_LayoutTeacher.cshtml";
    ViewBag.Title = " Describe FAQ";
}
<style>
    .divValidate {
        display: none;
        color: red;
    }
</style>
<div class="pt-32pt">
    <div class="container-fluid page__container d-flex flex-column flex-md-row align-items-center text-center text-sm-left">
        <div class="flex d-flex flex-column flex-sm-row align-items-center">
            <div class="mb-24pt mb-sm-0 mr-sm-24pt">
                <h2 class="mb-0">Describe FAQ</h2>
                <ol class="breadcrumb p-0 m-0">
                    <li class="breadcrumb-item"><a href="/Admin/Index">Home</a></li>
                    <li class="breadcrumb-item active">
                        Describe FAQ
                    </li>
                </ol>
            </div>
        </div>
    </div>


    <div class="page-section border-bottom-2">
        <div class="container-fluid page__container">

            <div class="row">
                <div class="col-md-12">

                    <div class="page-separator">
                        <div class="page-separator__text">Basic information</div>
                    </div>
                    @Html.ValidationSummary("", new { @class = "text-danger" })
                    @Html.HiddenFor(model => model.Type)
                    @Html.HiddenFor(model => model.Question_Id)
                    <label class="form-label">Question</label>
                    <div class="form-group mb-24pt">
                        @Html.TextBoxFor(model => model.Question, new { @class = "form-control form-control-lg", @placeholder = "Question", @required = "required" })
                        <div id="Questionval" class="divValidate"></div>
                    </div>

                    <div id="divAnswer">
                        <label class="form-label">Answer</label>
                        <div class="form-group mb-24pt">
                            @Html.TextAreaFor(model => model.Answer, new { @class = "form-control form-control-lg", @placeholder = "Answer", @rows = 5 })
                            <div id="Answerval" class="divValidate"></div>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="col-12 col-md-8 mb-3">
                            <label class="form-label">Tags</label>
                            @Html.TextBoxFor(model => model.Tags, new { @class = "form-control form-control-lg tags", @placeholder = "Tags for Search", @required = "required" })
                            @Html.ValidationMessageFor(model => model.Tags, "", new { @class = "text-accent-red" })
                            <small class="form-text text-muted">Separate keywords with a comma, space bar, or enter key</small>
                        </div>
                        <div class="col-12 col-md-4 mb-3">
                            <label class="form-label">Category</label>
                            @Html.DropDownListFor(x => x.CategoryID, new SelectList(ViewBag.Categories, "Master_ID", "Master_Value"), "Select Category", new { @class = "form-control  select2", @required = true })
                            <div id="CategoryIDval" class="divValidate"></div>
                        </div>
                        <div class="col-12 col-md-4 mb-3">
                            <label class="form-label">Show In Popular</label>
                            @Html.CheckBoxFor(x => x.IsPopular, new { @class = "form-control ", @required = true })
                            @Html.ValidationMessageFor(model => model.IsPopular, "", new { @class = "text-accent-red" })
                        </div>
                    </div>
                    <div class="card">
                        <div class="card-header text-center">
                            <a href="/Questions/FaqIndex" class="btn btn-outline-accent-electric-violet"><i class="fa fa-recycle"></i>&nbsp; New</a>
                            @if (Model.Question_Id > 0)
                            {
                                <button onclick="Save()" id="btnSave" class="btn btn-outline-accent-red">Update</button>
                            }
                            else
                            {
                                <button onclick="Save()" id="btnSave" class="btn btn-outline-success">Save</button>
                            }
                            <a href="@Url.Action("FaqList","Questions")" class="btn btn-outline-accent-dodger-blue"><i class="fa fa-backward"></i>&nbsp;  Back To List</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section scripts {
    @Scripts.Render("~/bundles/jqueryval")


    <script>
        $(document).ready(function () {
        })

        function Save() {

            var data = {
                Question_Id: $("#Question_Id").val(),
                Question: $("#Question").val(),
                Answer: $("#Answer").val(),
                Tags: $("#Tags").val(),
                IsPopular: $("#IsPopular").is(":checked"),
                CategoryID: $("#CategoryID").val(),
                AnsExplain: $("#AnsExplain").val(),
                DifficultyLevel: $("#DifficultyLevel option:selected").val(),
                Section_Fid: $("#Section_Fid").val(),
                Course_Fid: $("#Course_Fid").val(),
                Type: $("#Type").val(),
                Marks: $("#Marks").val(),
            }

            var result = ISPageValid(data);
            if (result) {
                $("#btnSave").hide();
                $.ajax({
                    url: '/Questions/Index',
                    method: 'POST',
                    data: JSON.stringify(data),
                    dataType: 'Json',
                    contentType: 'application/json',
                    success: function (response) {
                        if (response.Question_Id > 0) {
                            DataSaved();
                            $("#btnSave").attr("disabled", true);
                        }
                    },
                    error: function (err) {

                    }
                });
            }
        }

        function Preview() {
            if ($("#Question_Id").val() > 0)
                window.location.href = "/Questions/Preview/" + $("#Question_Id").val();
        }

        function ISPageValid(data) {
            $(".divValidate").hide();
            if (data.CategoryID == "" || data.Question == "" || data.Answer == "") {
                if (data.CategoryID == "") $("#CategoryIDval").show().text("Required");
                if (data.Question == "") $("#Questionval").show().text("Required");
                if (data.Answer == "") $("#Answerval").show().text("Required");
                return false
            }
            else
                return true
        }

        $(document).on("click", "#btnDelete", function () {
            var btn = $(this);
            var id = btn.data("id");
            var Name = btn.data("name");
            Swal.fire({
                title: 'Are you sure?',
                text: "You Want To Delete the FAQ '" + Name + "' ?",
                icon: 'warning',
                showCancelButton: false,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                confirmButtonText: 'Yes!'
            }).then((result) => {
                if (result.isConfirmed) {
                    $.ajax({
                        url: "/Questions/DeleteQuestion/" + id,
                        method: "GET",
                        beforeSend: function () {
                            swal.fire({
                                html: '<h5>Deleting FAQ...</h5>',
                                showConfirmButton: false,
                                onRender: function () { $('.swal2-content').prepend(sweet_loader); }
                            });
                        },
                        success: function (result) {
                            if (result) {
                                btn.hide();
                                Swal.fire(
                                    'Delete!',
                                    'The FAQ ' + Name + ' Has Been Deleted.',
                                    'success'
                                )
                                window.location.href = "/Questions/List?Type=" + $("#Type").val();
                            }
                        }, error: function (err) {
                            alert("Error" + err);
                        }
                    })

                }
            })
        })
    </script>

}

